import { Row } from 'antd';
import styled from 'styled-components';

export const WrapperStyled = styled(Row)`
  .col-create {
    margin-bottom: 20px;
  }

  .icon-action {
    margin-right: 10px;
  }

  .icon-delete {
    color: red;
  }
`;
